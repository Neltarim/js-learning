// declare variable
let number; //doesn't need a value

//with a value
let nbr = 2;

//modify a variable
nbr = 3;

// + - or += -= is used the same way as python
// *= and /= are also availables


// ++ and -- can be done (the old friends)


// constantes
const QIofKirby = 143;